# 轴类（TURNING）零件路线开发方案

> 目标：在不改大类路由、不影响钣金 / 大板 / 方件的前提下，通过在 `TURNING` 族内部增加「轴类子画像 AxisProfile」+ 规则分流，从根本上修复 13 件轴类样本中的 7 件差异。
> 适用范围：仅 `family_hint == TURNING`。所有改动均以此为硬门禁。

---

## 1. 背景

轴类 13 件抽测结果：系统与人工**完全一致 6/13**，**需复核或有差异 7/13**。

| 件号 | 差异 | 归类 |
|---|---|---|
| RM-JJ-00083648-01 | 长细滚筒，缺校直 / 跳动复核（L/D≈14.72 卡阈值下沿） | 校直阈值过硬 |
| RM-JJ-00083816-01 | 薄垫圈/隔套被套圆磨 + 默认硬铬遮蔽 | 子类型缺失 + 表处过通用 |
| RM-JJ-00083848-01 | 同上 | 同上 |
| RM-JJ-00083858-01 | 同上 | 同上 |
| RM-JJ-00083824-01 | 动力轴提示复杂轴面风险，但未显式列 `shaft_milling` | 复杂轴面证据识别不足 |
| RM-JJ-00084164-01 | 多出 `stress_relief`，应并入调质热处理复核 | 去应力与热处理未解耦 |
| RM-JJ-00084311-01 | D37 深盲腔未单列镗孔 / 内孔加工 | 缺大内孔→镗孔映射 |

**根本判断**：不是「轴类整体路由错」，而是「轴类内部没有再细分」。系统把圆件统一路由到 `TURNING`，但 `TURNING` 内部混着长轴、滚筒、薄垫圈、隔套、定位圈、带大内孔滚筒、多孔长轴，它们共用同一套轴类证据规则，所以边界件出偏差。

---

## 2. 根因（含代码锚点）

| # | 根因 | 代码位置 | 现状 |
|---|---|---|---|
| R1 | 轴类无子类型 | `evidence.py` 整个 TURNING 证据链 | 只知 `TURNING`，不分长轴/薄环/滚筒 |
| R2 | 圆磨对薄环误泛化 | `evidence.py` `_add_finishing_evidence` 的 `turning_finish_precision` → `cylindrical_grinding`（约 1130–1145） | 任何带精度的 TURNING 件都给 `cylindrical_grinding` |
| R3 | 校直阈值过硬 + 缺轴类语义词 | `evidence.py` `_add_shaft_straightness_detailing`（slender = 最长边≥200 且 L/D≥15）；`process_recognition.py:320` `STRAIGHTENING_KEYWORDS=("校平","校直","straighten")`（全局词表，不宜直接扩） | 83648 = 368/25 = 14.72 < 15，且「避免弯曲/合理支撑」不在词表 → 静默跳过 |
| R4 | 复杂轴面证据识别不足 | `evidence.py` `_add_shaft_milling_evidence`(688) + `_is_complex_shaft_surface`(778) | **分流逻辑已实现**（弱证据→review、真证据→入路线）；缺的是 `complex_shaft_surface` 检测阈值，导致 83824 不触发任何信号 |
| R5 | 缺大内孔→镗孔映射 | 孔规则偏小孔/螺纹/沉孔。**`boring` 工序已存在**：`process_dictionary.py:646` 完整定义（镗孔，类别精孔加工，`pricing_unit="hole"`，`quantity_type="precision_hole_count"`），并已接入 `process_recognition.py` 阶段链（`finish_and_precision`/`post_heat_correction`）与精孔分组；**仅未显式列入 `TURNING_MATRIX`**（机制 `_with_surface(base, CONDITIONAL)`，未列工序默认 `CONDITIONAL`，即有证据即允许） | 84311 的 D37 深盲腔只被当车削主线，缺触发规则 |
| R6 | 去应力与热处理未解耦 | `evidence.py` `_add_finishing_evidence` 的 `trigger_stress`；`process_recognition.py` `THERMAL_DISTORTION_MATERIALS`(含 45/40Cr) | 45/40Cr + 调质 → 额外 `stress_relief`（84164） |
| R7 | 硬铬遮蔽过通用 | `evidence.py` 表处链（硬铬默认加 `hard_chrome_masking`） | 薄环非局部镀也默认遮蔽 |

---

## 3. 设计总原则

> **几何族（TURNING）只决定主加工骨架；孔精度、表处链、热处理、校直等特征工序由特征证据独立判定。子画像只用于分流，且必须可回退到复核——绝不静默删/换工序。**

落地为一个新结构 **AxisProfile**，在进入轴类证据层时一次性计算，整条轴类规则链共用。**子画像是一个会出错的分类器**，因此凡是「会删除或替换工序」的分流，置信度不足时一律回退到当前行为 + 复核。

---

## 4. AxisProfile 子画像规格

入口：`family_hint == TURNING` 时计算；其它族不进入此函数，`CATEGORY_TO_FAMILY` 不动。

字段：

| 字段 | 含义 | 来源 |
|---|---|---|
| `turning_subtype` | 见下方分桶 | 几何 + 复杂度 |
| `slenderness` | 长径比 L/D | bounding_box |
| `is_thin_ring` | 厚度远小于外径（端面/厚度主导） | bounding_box |
| `has_large_coaxial_bore` | 大直径同轴盲/通孔 + 深腔 | 孔标注 + 几何 |
| `has_thread` | 是否有螺纹 | 孔标注 |
| `has_real_milling_feature` | 键槽/扁位/腰孔/横孔/侧孔真实证据 | `_add_shaft_milling_evidence` 已有信号 |
| `surface_scope_confidence` | 表处是否明确为局部镀 | 表处 raw_text |
| `subtype_confidence` | 子类型置信度（几何完整度） | 维度可用数量 |

**子类型分桶（刻意做粗，降低误分面）**：
最终只对应两个磨削出口，故收敛为：

- `SLENDER_OR_ROLLER`（长轴 / 滚筒 / 外圆工作面）→ 走 `cylindrical_grinding`、校直
- `SHORT_RING`（薄垫圈 / 短隔套 / 定位圈）→ **默认** `finish_grinding` / 端面·厚度精整、禁用校直；但**不绝对禁圆磨**——有明确外圆精度 / 镀后修磨 / 外圆配合面证据时，仍允许 `cylindrical_grinding`（避免误删短套、定位圈关键外圆的圆磨）
- `UNKNOWN`（维度不足 / `subtype_confidence` 低）→ **回退当前行为 + 复核**，不主动改路线

`has_large_coaxial_bore`、`has_thread`、`has_real_milling_feature` 为正交特征标记，独立驱动镗孔 / 螺纹链 / 铣削，不依赖子类型。

---

## 5. 详细开发项

### 项 2 — 磨削分流（修 83816 / 83848 / 83858）
**改动**：`_add_finishing_evidence` 内、`family_hint == TURNING` 分支，把 `cylindrical_grinding` 的触发改为按 `turning_subtype` 分流：
- `SLENDER_OR_ROLLER`：保留 `cylindrical_grinding`
- `SHORT_RING`：**默认**改 `finish_grinding`（**已在 `TURNING_MATRIX` 为 CONDITIONAL，无需改矩阵**）或端面/厚度精整；**例外**——命中外圆精度 / 外圆配合面 / 镀后外圆修磨证据时，仍出 `cylindrical_grinding`（不绝对禁用）
- `UNKNOWN`：维持现状 + 复核

**隔离**：仅改 TURNING 分支；`turning_finish_precision` 的其它用途（如 has_precision 计算）不动。

**验收**：3 件薄环（无外圆配合证据）断言不出 `cylindrical_grinding`、出端面/厚度精磨；一个外圆为关键配合面的短套断言**仍出** `cylindrical_grinding`；一根长轴断言仍出 `cylindrical_grinding`。

### 项 3 — 校直分级 + 轴类专用词表（修 83648）
**改动**：
1. `_add_shaft_straightness_detailing` 改分级：`L/D≥15` 直接加 `straightening`；`12≤L/D<15` 且命中轴类校直语义或有热处理/硬铬 → 加 `straightening` 且 `requires_review=True`；`SHORT_RING` 子类型禁用本规则。
2. **新增轴类专用词表，不动全局 `STRAIGHTENING_KEYWORDS`**：定义 `AXIS_STRAIGHTNESS_KEYWORDS = ("避免弯曲","合理支撑","直线度","同轴度", ...)`，**仅在 `_add_shaft_straightness_detailing` 且 `family_hint == TURNING` 时消费**。这样大板/钣金/旧版规则完全不受影响，规避了改全局词表的扩散风险。

**隔离**：专用词表只在轴类校直分支引用，零跨族影响。短件经子类型禁用，不受影响。

**验收**：83648（14.72 + 「避免弯曲」）出 `straightening`(review)；一根 L/D=10 的普通短轴断言不出。边界用例 L/D 正好 12、15。

### 项 4 — 复杂轴面：修「检测」，不要重写「分流」
**现状**：`_add_shaft_milling_evidence`(688) 已实现你要的分流——`complex_surface_candidate` 无真实证据 → 仅 `SHAFT_SURFACE_REVIEW`(review_only)；有键槽/扁位/腰孔/横孔 → 加 `shaft_milling`/`keyway_milling`/`cross_drilling`。**不要重复实现这套。**

**真正改动**：调 `_is_complex_shaft_surface`(778)，但**不能只降 `complexity_score`**——单降阈值会让普通光轴 STEP 面数稍多时误出轴铣。改成**组合条件全部/多数成立才触发**：① 长径比像轴；② `complexity_score` 中高；③ face/edge/hole 复杂度异常；④ 不是 `SHORT_RING`；⑤ 至少叠加孔 / 局部非回转面 / PDF 特征词之一。满足组合 → `complex_shaft_surface=True` → 进入复核（`requires_review=True`）。

**隔离**：阈值只影响 TURNING；用现有 6 件正确样本回归，确认不把普通光轴误判出铣削。

**验收**：83824 出轴上铣削复核工序（review）；普通光轴（面数偏多但无孔/非回转面/特征词）断言**不**出。

### 项 5 — 大内孔 → 镗孔（修 84311）
**`boring` 不是新增工序**：已在 `process_dictionary.py:646` 完整定义（镗孔，`quote_process_code="precision_hole"`），并接入阶段链/精孔分组。`TURNING_MATRIX` 未显式列但默认 `CONDITIONAL`（有证据即允许），可保持默认或为可读性显式列入。

**⚠️ 报价闭环已核实存在缺口（落地前必补）**：最小用例（路线仅含 `boring` + 2 个 D37 深盲孔）实跑结果 ——
```
precision_hole quantity=None unit_price=None amount=None
risk: MISSING_PRICE_OR_QUANTITY 精孔加工缺少单价或工程量
```
根因在 `pricing_core.py` `add_hole_quantities`(963) 的两道门：
1. **计数只认四类 op**：`counts` 仅 `drilling/countersink/tapping/precision_hole`，且累加来源是 `hole_type`（`precision_candidate`→`precision_hole`）。`boring` 不在其中，D37 深盲腔常被解析成 `blind`（只进 `drilling` 计数），连精孔 count 都进不去。
2. **发射数量按 `operation_code in operations` 过滤**(1037)：报价侧虽把 `boring` 用 `quote_process_code` 合并显示为 `precision_hole`，但数量循环找的是路线里有没有 `precision_hole` 这个 op；路线只有 `boring` → 跳过 → 数量缺失 → `MISSING_PRICE_OR_QUANTITY`。

**落地动作（两处，均不新增工序）**：
1. **路线触发**：TURNING 证据层补"大内孔/深盲腔 → `boring`"。触发条件须组合：`hole_diameter/outer_diameter ≥ 0.4` **且**（深盲腔/同轴腔体信号）。⚠️ 仅靠「孔径≥20mm」会误伤薄环/隔套（内孔天生大、本由车削产生），必须叠加深腔信号。
2. **数量映射（必补）**：在 `add_hole_quantities` 把 `boring` 纳入 `counts/sources/review_flags/quantity_specs`（或新增更明确的 `boring_count`）。**关键：count 来源取 AxisProfile 的"大内孔/深盲腔候选"（`has_large_coaxial_bore`），而非 `precision_candidate` hole_type**，否则 `blind` 大孔进不了。单价继续复用 `precision_hole` 体系；数量按检测到的大孔数计 `pcs`（2 个 D37 = 2 pcs），默认 `requires_review=True`。

**隔离**：仅 `family_hint == TURNING`；不改 PDF/STEP 原始孔解析；不改报价单价规则，只补数量来源。

**验收**：84311（2×D37 深盲腔）出 `boring`，且报价项有数量、无 `MISSING_PRICE_OR_QUANTITY`；一个大内孔薄隔套（浅孔/无深腔）断言**不**误加镗孔。

### 项 6 — 硬铬遮蔽改条件（修 83816 等表处过度）
**改动**：轴类表处链中 `hard_chrome_masking` 区分"工序"与"复核风险"，三档：
- 明确局部镀 / 非镀面 / 螺纹 / 精孔 → 加入 `hard_chrome_masking`（工序）
- 普通整体镀硬铬（无局部镀证据） → 不加工序，给 `HARD_CHROME_MASKING_REVIEW`（复核风险）
- `SHORT_RING` → 默认不加遮蔽，仅复核

`thread_chasing`（有螺纹+表处）保留；镀后抛光对 `SLENDER_OR_ROLLER` 保留、对 `SHORT_RING` 标复核。

**隔离**：阳极/化镍/其它表处链不动；仅调硬铬分支且限 TURNING。

**验收**：薄环硬铬件不默认出 `hard_chrome_masking`（出复核）；带螺纹的长轴硬铬件仍出遮蔽。

### 项 7 — 去应力与热处理解耦（修 84164）
**改动**：轴类 `trigger_stress` 改为——图纸明确「去应力」→ 加；长细轴 + 热处理 + 高变形风险 → 加(review)；普通调质/淬火已由 `heat_treatment` 表达时**不默认另加** `stress_relief`。与大板问题 3 同一原则（去应力不被几何/热处理顺手带出）。

**隔离**：`trigger_stress` 的其它族路径不动；仅在 TURNING 分支按上述收紧。

**验收**：84164（45/40Cr+调质，无去应力字样）不出独立 `stress_relief`，并入热处理复核；一根明确标「去应力」的轴仍出。

---

## 6. 影响隔离保证（三条硬边界，逐项自查）

1. **族门禁**：所有新逻辑写在轴类专用函数，入口 `family_hint == TURNING`。会删/换工序的分流，`subtype_confidence` 低时回退现状 + 复核。
2. **不动全局**：不改 PDF/STEP 原始解析；不改全局工序字典与钣金/大板/方件矩阵；不改报价单价规则。项 5 复用现有 `boring`，**已核实须在 `add_hole_quantities` 补 `boring` 数量映射**（仅补数量来源，不新增工序、不改单价）。校直语义改用**轴类专用词表**，不动全局 `STRAIGHTENING_KEYWORDS`。
3. **回归护栏**：新增轴类专项快照（13 件黄金样本），同时全量保留现有非轴类快照（`test_route_snapshot.py` / `test_route_snapshot_batch4.py`），确保其它族路线零变化。

---

## 7. 测试与验收方案

**黄金样本**（`route_engine_fixtures.py` 扩充）：6 件已对的钉为回归基线（断言路线不变）；7 件差异件断言改后 == 人工路线。

**反例 / 边界合成用例**（防止把规则改激进）：
- 校直：L/D = 10 短轴（不出）、L/D 正好 12 与 15（边界行为明确）
- 圆磨：带精孔的薄环（断言出 `finish_grinding`，**不**出 `cylindrical_grinding`）
- 镗孔：大内孔但浅的隔套（断言**不**出镗孔）
- 铣削：普通光轴（断言**不**出 `shaft_milling`）
- 去应力：调质但无去应力字样（不出独立 `stress_relief`）；明确标去应力（出）

**非轴类回归**：钣金/大板/方件现有快照全绿。

通过门槛：7 差异件对齐人工 + 6 基线件不回归 + 全部反例符合预期 + 非轴类快照零变化。

---

## 8. 实施顺序与里程碑

| 阶段 | 内容 | 依赖 |
|---|---|---|
| M1 | 实现 AxisProfile：**只识别、输出画像/日志，不改路线输出** + 单元测试（验证 13 件维度可用率与子类型判定） | — |
| M2 | 低风险三项：项 2 薄环/短套磨削分流、项 3 校直分级（轴类专用词表）、项 7 去应力与热处理解耦（纯证据层，零矩阵改动） | M1 |
| M3 | 中风险两项：项 6 硬铬遮蔽条件化、项 4 复杂轴面复核/轴铣触发收紧（组合条件） | M1 |
| M4 | 大内孔（**报价闭环已核实有缺口，见下**）：① TURNING 证据层补"大内孔/深盲腔 → `boring`"触发；② **必补** `add_hole_quantities` 的 `boring` 数量映射；不新增工序 | M1 |
| M5 | 测试门槛：13 件黄金样本 + 合成反例（短轴不校直、薄环不圆磨、普通光轴不轴铣、大浅孔不镗孔）+ **`boring` 报价闭环用例（出 boring 时数量非空、无 `MISSING_PRICE_OR_QUANTITY`）** + 非轴类快照零变化 | M2–M4 |

M1 作为去风险动作先行（只观测、不改输出，验证子画像可靠性）；再做 M2（收益高、零边界风险），M3/M4 跟进。

---

## 9. 风险与未决项

- **已核实（报价闭环缺口）**：项 5 复用现有 `boring`，但 `add_hole_quantities` 不含 `boring`/大孔数量来源，路线一旦出 `boring` 会报 `MISSING_PRICE_OR_QUANTITY`。M4 必须同步补数量映射（count 取大内孔候选、复用 precision_hole 单价、默认复核）。这是落地前唯一必须补的报价闭环。
- **风险**：AxisProfile 依赖几何维度，STEP/PDF 维度缺失会误分子类型 → 已用「低置信度回退复核」兜底，并在 M1 以"只识别不改输出"先行验证 13 件维度可用率。
- **已规避**：校直语义改用轴类专用 `AXIS_STRAIGHTNESS_KEYWORDS`，不动全局词表，消除跨族扩散风险。
- **风险**：项 2「外圆配合面证据」与项 4「复杂轴面组合条件」的判定依赖证据质量，需在 M2/M3 用反例锁边界，防止误删圆磨 / 误加轴铣。
- **范围确认**：13 件样本偏小，锁分类器能力有限；建议后续补充更多轴类样本扩充黄金集。
