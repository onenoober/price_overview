# 核价计算模块

## 模块目标

根据工程量、价格规则、风险附加和报价参数生成 `quote_result`。系统必须先生成初始报价，再由人工调整确认。

## 主责

开发者 A 主责。

## 输入

| 输入 | 说明 |
|---|---|
| `part_feature` | 材料、几何、风险。 |
| `process_route` | 工序路线。 |
| `quantity_result` | 工程量。 |
| `price_rule` | 价格规则。 |
| 报价参数 | 税率、固定管理费规则、取整规则。 |

## 输出

| 输出 | 说明 |
|---|---|
| `quote_result` | 报价结果。 |
| `quote_item` | 报价明细。 |
| `quote_summary` | 汇总。 |
| 风险 | 价格缺失、价格异常、必确认项。 |

## 报价公式

```text
材料费 = 毛重 × 材料单价
加工费 = Σ max(工序工程量 × 工序单价, 工序起步价)
表面处理费 = Σ(表面处理工程量 × 表面处理单价)
管理费 = 材料费 × 5%（固定规则）
税金 = (加工费 + 表面处理费 + 管理费) × 税率
系统计算价 = 材料费 + 加工费 + 表面处理费 + 管理费 + 税金
系统初始报价 = 系统计算价按取整规则和报价规则生成的可编辑报价
最终确认价 = 报价员确认或修改后的价格
```

材料单价可使用实时市场搜索生成待复核价格；加工工序单价第一版使用 `华南工序计价标准表`，不在每次报价中依赖实时网络搜索。化学镍属于表面处理费，不进入加工费。表面处理单价优先使用表面处理价格库；价格库缺失时使用 Tavily + GPT 搜索候选价，只有计价单位与当前工程量单位匹配时才参与计算。若价格库和市场搜索均无可用单价，可使用 GPT-only 估算价生成第一版报价，但必须标记需复核，且不得标记为实时市场价。

## 华南工序计价标准表

| 工序 | 工程量来源 | 计价单位 | 第一版取值 | 起步价 | 备注 |
|---|---|---:|---:|---:|---|
| 锯切下料 | 件数/刀数 | 件 | 10 元/件 | 8 元 | 第一版按零件数量作为待复核刀数。 |
| CNC铣削 | 估算工时 | h | 115 元/h | 80 元 | 取 80-150 元/h 区间中值。 |
| 平面磨 | 工时/面积 | h | 90 元/h | 20 元 | 面积转工时规则待补充。 |
| 钻孔 | 孔数量 | 孔 | 3.5 元/孔 | 10 元 | 取 2-5 元/孔区间中值。 |
| 沉孔 | 沉孔数量 | 孔 | 10 元/孔 | 8 元 | 取 5-15 元/孔区间中值。 |
| 攻牙 | 螺纹孔数量 | 孔 | 10 元/孔 | 10 元 | 螺纹规格需复核。 |
| 精孔 | 精孔数量 | 孔 | 27.5 元/孔 | 20 元 | H7、E8、G6 等配合孔。 |
| 线切割 | 切割面积 | mm² | 0.02 元/mm² | 40 元 | 默认按中走丝口径。 |
| 热处理 | 重量/炉次 | kg | 16.5 元/kg | 30 元 | 小件按炉次摊销需复核。 |
| 去毛刺 | 件数/边复杂度 | 件 | 12.5 元/件 | 5 元 | 复杂外形加价需复核。 |
| 检验包装 | 件数 | 件 | 17.5 元/件 | 10 元 | 终检和防护包装合并计价，避免重复收费。 |
| 化学镍 | STEP 表面积 | m² | 表面处理价格库、Tavily + GPT 候选价或 GPT-only 估算价 | 0 元 | 单位必须匹配 m²；GPT-only 估算价只用于第一版报价，必须复核。 |

## 报价明细类型

| 类型 | 说明 |
|---|---|
| `material` | 材料费。 |
| `process` | 加工工序费。 |
| `surface_treatment` | 表面处理费。 |
| `management_fee` | 管理费。 |
| `tax` | 税金。 |
| `risk_surcharge` | 风险附加。 |
| `other` | 其他人工补录项。 |

## 报价项必须包含

| 字段 | 说明 |
|---|---|
| `item_id` | 明细 ID。 |
| `item_type` | 明细类型。 |
| `operation_code` | 关联工序，可为空。 |
| `quantity` | 工程量。 |
| `unit` | 单位。 |
| `unit_price` | 单价。 |
| `amount` | 金额。 |
| `price_source` | 价格来源。 |
| `formula` | 计算公式。 |
| `explanation` | 说明。 |

## 状态规则

| 条件 | 报价状态 |
|---|---|
| 无必确认风险 | `priced` |
| 有必确认风险 | `pending_review` |
| 人工确认完成 | `confirmed` |
| 作废 | `voided` |

## 人工修改

人工修改必须通过 `human_override` 保存。

要求：

| 要求 | 说明 |
|---|---|
| 原始值保留 | 系统初始值不得被覆盖。 |
| 原因必填 | 所有修改必须填写原因。 |
| 修改可追踪 | 保存操作人和时间。 |
| 可对比 | 初始报价和最终确认价可对比。 |

## 风险处理

| 风险 | 处理 |
|---|---|
| `MISSING_PRICE` | 可生成初始报价但缺失项必须待确认。 |
| `WEIGHT_MISMATCH` | 状态进入 `pending_review`。 |
| `UNKNOWN_MATERIAL` | 不得使用默认材料价。 |
| `PRICE_OUTLIER` | 提示复核。 |
| `HIGH_PRECISION_REQUIREMENT` | 可添加风险附加或待确认。 |

## 不做什么

- 不解析 PDF 或 STEP。
- 不让 AI 决定价格。
- 不自动确认最终报价。
- 不修改价格规则表。
- 不覆盖系统初始报价。

## 验收标准

- 报价明细可解释率 100%。
- 价格来源可追溯。
- 人工修改记录完整率 100%。
- 有 blocking 风险不得确认。
- `quote_result` 通过 Schema 校验。

## AI Coding 注意事项

- 不要把报价公式散落在前端。
- 不要在价格缺失时生成看似正常的总价。
- 不要用浮点误差影响金额汇总。
- 不要在人工修改后丢失系统初始报价。

