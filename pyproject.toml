[project]
name = "price-overview"
version = "0.1.0"
description = "Workpiece pricing system mock pricing core"
requires-python = ">=3.10"
dependencies = [
  "jsonschema>=4.0.0"
]

[tool.setuptools.packages.find]
where = ["src"]

